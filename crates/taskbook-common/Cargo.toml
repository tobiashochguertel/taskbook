[package]
name = "taskbook-common"
version = "1.12.0"
edition = "2021"
rust-version = "1.75"
description = "Shared types for taskbook"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"
thiserror = "1"
aes-gcm = "0.10"
rand = "0.8"
